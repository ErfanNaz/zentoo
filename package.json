{
  "name": "zentoo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "clean": "rm -rf dist/*",
    "release": "npm run clean && npm run build && cp CNAME ./dist",
    "deploy": "git push origin :gh-pages && git subtree push --prefix dist origin gh-pages",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "animate.css": "^4.1.1",
    "bootstrap": "^3.3.7",
    "font-awesome": "^4.7.0",
    "hamburgers": "^1.1.3",
    "jquery": "^3.6.0",
    "jquery-smooth-scroll": "^2.2.0",
    "parcel-bundler": "^1.12.5"
  },
  "devDependencies": {
    "sass": "^1.43.2",
    "typescript": "^4.4.4"
  }
}
